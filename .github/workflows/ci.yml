name: CI

on:
  push:
    branches: [main, dev]
  pull_request:

env:
  GOPROXY: https://goproxy.cn,direct

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./... -skip='Live'
